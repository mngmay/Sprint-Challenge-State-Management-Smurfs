const initialState = {
  name: "",
  age: "",
  height: "",
  id: ""
};

export const smurfReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
